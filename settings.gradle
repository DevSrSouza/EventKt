rootProject.name = 'EventKt'
include(
        ":core",
        ":remote-core",

        // encoders
        ":remote-encoder-serialization",

        // remote clients
        ":remote-jvm-jedis",
        ":remote-jvm-rabbitmq"
)

enableFeaturePreview('GRADLE_METADATA')
